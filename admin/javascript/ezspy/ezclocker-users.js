var url = '../users';
function getUser(username, success, fail) {
    ezGetJSON(url, '', success, error, async);
}

function putUser(id, success, fail) {
    ezGetJSON(url, payload, success, error, async);
}

function deleteUser(id, success, fail) {
    ezGetJSON(url, payload, success, error, async);
}

function postUser(username, password, fullname, success, fail) {
    ezGetJSON(url, payload, success, error, async);
}